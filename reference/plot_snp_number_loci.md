# Figure of the distribution of SNP per locus before and after filters

Create density distribution of SNP per locus before and after filters.

## Usage

``` r
plot_snp_number_loci(before.filter.data, after.filter.data)
```

## Arguments

- before.filter.data:

  Data set before filter.

- after.filter.data:

  Data set after filter.
