% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_hwe_gds.R
\name{filter_hwe}
\alias{filter_hwe}
\title{Filter Hardy-Weinberg disequilibrium}
\usage{
filter_hwe(
  data,
  strata = NULL,
  group.column = "STRATA",
  p.threshold = 1e-04,
  strata.threshold = 1L,
  adjustment = c("BH", "none"),
  min.samples = 10L,
  min.call.rate = 0.8,
  chunk.size = 2000L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A GDS filepath or open \code{SeqVarGDSClass} object.}

\item{strata}{Optional metadata containing \code{INDIVIDUALS} and \code{group.column}.}

\item{group.column}{Metadata column defining strata.}

\item{p.threshold}{Mid-p or adjusted-p threshold.}

\item{strata.threshold}{Number or proportion of eligible strata that must
depart before marker removal.}

\item{adjustment}{Multiple-testing method: \code{"BH"} or \code{"none"}. Adjustment
is performed independently within each stratum.}

\item{min.samples}{Minimum samples in a stratum.}

\item{min.call.rate}{Minimum marker call rate within a stratum.}

\item{chunk.size}{Number of variants read at a time.}

\item{verbose}{Display progress and summary messages.}

\item{...}{Common argument \code{path.folder}.}
}
\value{
The filtered open GDS connection with evidence and audit files.
}
\description{
Apply exact mid-p Hardy-Weinberg tests to active biallelic GDS markers within
strata and remove markers departing from equilibrium in enough eligible
strata. Tests use current active samples and are recalculated on every run.
}
\details{
HWE departures are not automatically technical failures. Population
mixture, selection, inbreeding, sex linkage, null alleles, paralogy, and
genotyping error can all generate departures. This function is a deliberate
filter with full evidence tables, not a substitute for diagnosis.
}
\examples{
\dontrun{
genome <- radr::filter_hwe(
  genome, strata = "sample_metadata.tsv", p.threshold = 1e-4,
  strata.threshold = 2
)
}
}
\references{
Wigginton JE, Cutler DJ, Abecasis GR (2005). A note on exact
tests of Hardy-Weinberg equilibrium. \emph{AJHG}, 76, 887-893.

Graffelman J, Moreno V (2013). The mid p-value in exact tests for
Hardy-Weinberg equilibrium. \emph{SAGMB}, 12, 433-448.
}
\author{
Thierry Gosselin \email{thierrygosselin@icloud.com}
}
