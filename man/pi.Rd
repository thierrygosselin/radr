% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pi.R
\name{pi}
\alias{pi}
\title{Estimate nucleotide diversity}
\usage{
pi(
  data,
  strata = NULL,
  group.column = "STRATA",
  scale = c("per_variant", "per_base"),
  callable.bases = NULL,
  chunk.size = 2000L,
  write.files = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A GDS filepath or open \code{SeqVarGDSClass} object.}

\item{strata}{Optional metadata containing \code{INDIVIDUALS} and \code{group.column}.}

\item{group.column}{Metadata column defining strata.}

\item{scale}{Either \code{"per_variant"} or \code{"per_base"}.}

\item{callable.bases}{Positive callable-base denominator used with
\code{scale = "per_base"}. A scalar applies to all groups; a named vector can
provide group-specific denominators.}

\item{chunk.size}{Number of variants read at a time.}

\item{write.files}{Write result tables and a plot.}

\item{verbose}{Display progress and summary messages.}

\item{...}{Common arguments: \code{path.folder} and \code{internal}.}
}
\value{
A \code{pi} object containing \code{marker.statistics},
\code{population.statistics}, \code{individual.statistics}, \code{plot}, and output
information.
}
\description{
Estimate nucleotide diversity (pi), the mean number of pairwise nucleotide
differences, from active biallelic variants in a GDS. Estimates are
calculated independently for each stratum and for the pooled sample using
the finite-sample correction \verb{2n / (2n - 1)}.
}
\details{
With \code{scale = "per_variant"}, pi is averaged across active variant records.
This describes diversity among the analysed sites and is not a genome-wide
per-base estimate. With \code{scale = "per_base"}, the sum of pairwise
differences is divided by \code{callable.bases}; this denominator must come from
an appropriate callable-genome or callable-target analysis.
}
\examples{
\dontrun{
diversity <- radr::pi("study.gds", strata = "sample_metadata.tsv")
}
}
\references{
Nei M, Li WH (1979). Mathematical model for studying genetic
variation in terms of restriction endonucleases. \emph{PNAS}, 76, 5269-5273.
}
\author{
Thierry Gosselin \email{thierrygosselin@icloud.com}
}
