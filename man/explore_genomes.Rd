% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explore_genomes.R
\name{explore_genomes}
\alias{explore_genomes}
\title{Explore and filter genomic data}
\usage{
explore_genomes(
  data,
  interactive.filter = TRUE,
  output = NULL,
  filename = NULL,
  verbose = TRUE,
  parallel.core = parallel::detectCores() - 1,
  ...
)
}
\arguments{
\item{data}{A GDS filepath or an open \code{SeqVarGDSClass} object. Other genomic
formats must first be imported with an appropriate \code{genometranslator}
reader, such as \code{\link[genometranslator:read_vcf]{genometranslator::read_vcf()}},
\code{\link[genometranslator:read_dart]{genometranslator::read_dart()}}, or \code{\link[genometranslator:read_plink]{genometranslator::read_plink()}}.}

\item{interactive.filter}{Logical. If \code{TRUE}, figures and questions guide
the selection of filtering thresholds. If \code{FALSE}, thresholds are supplied
through \code{...}. Default: \code{interactive.filter = TRUE}.}

\item{output}{Optional output format passed to
\code{\link[genometranslator:genome_translator]{genometranslator::genome_translator()}}. If \code{NULL}, no additional genomic
format is generated. Default: \code{output = NULL}.}

\item{filename}{Optional prefix used for generated output files.
Default: \code{filename = NULL}.}

\item{verbose}{Logical. Display progress messages.
Default: \code{verbose = TRUE}.}

\item{parallel.core}{Number of processor cores available to compatible
operations.
Default: \code{parallel.core = parallel::detectCores() - 1}.}

\item{...}{Filtering thresholds and advanced options passed to the individual
\code{radr} filtering and detection functions.}
}
\value{
A named list containing:
\describe{
\item{gds}{The filtered GDS object.}
\item{output}{The object generated by
\code{\link[genometranslator:genome_translator]{genometranslator::genome_translator()}}, or \code{"not selected"} when no
output format was requested.}
}
}
\description{
\strong{\code{explore_genomes}} provides an interactive, opinionated workflow for
exploring genomic quality metrics and applying an initial sequence of
filters. It is intended as a starting point, especially for users who are
learning about their dataset and selecting filtering thresholds.

The workflow is not a universal filtering recipe. The appropriate filters,
thresholds, and order depend on the organism, sequencing design, marker
type, and study objectives. Experienced users should generally build a
dataset-specific workflow from the individual \verb{filter_*()} and \verb{detect_*()}
functions in \code{radr}.
}
\details{
The standard workflow considers DArT reproducibility when available,
monomorphic and common markers, individual quality, allele frequency,
coverage, genotyping rate, SNP position and number, linkage disequilibrium,
mixed and duplicate genomes, and Hardy-Weinberg equilibrium. Steps can be
enabled, disabled, or configured through \code{...}.

The function creates a dated workflow folder containing the parameter log,
diagnostic figures, blacklists and whitelists, final metadata, and summary
statistics. Statistics are generated for these files and are not retained as
a separate object in the returned value.
}
\section{Interactive and tailored workflows}{

Interactive mode is designed to help users inspect diagnostic plots and
understand the consequences of candidate thresholds. For reproducible batch
analyses, use \code{interactive.filter = FALSE} and provide explicit thresholds.
For mature analyses, call the relevant functions individually so their order
and configuration reflect the dataset.
}

\examples{
\dontrun{
# Import a VCF with genometranslator, then explore it with radr
gds <- genometranslator::read_vcf(
  data = "shark.vcf",
  strata = "shark_strata.tsv"
)

shark <- radr::explore_genomes(data = gds)

# Experienced users can construct a tailored workflow instead
filtered <- gds |>
  radr::filter_genotyping(
    interactive.filter = FALSE,
    filter.genotyping = 0.20
  ) |>
  radr::filter_ma(
    interactive.filter = FALSE,
    filter.ma = 2
  )
}

}
\seealso{
\code{\link[=filter_individuals]{filter_individuals()}}, \code{\link[=filter_ma]{filter_ma()}}, \code{\link[=filter_coverage]{filter_coverage()}},
\code{\link[=filter_genotyping]{filter_genotyping()}}, \code{\link[=filter_ld]{filter_ld()}}, \code{\link[=detect_mixed_genomes]{detect_mixed_genomes()}},
\code{\link[=detect_duplicate_genomes]{detect_duplicate_genomes()}}, and
\code{\link[genometranslator:genome_translator]{genometranslator::genome_translator()}}.
}
\author{
Thierry Gosselin \email{thierrygosselin@icloud.com}
}
