% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_het_outliers_gds.R
\name{detect_het_outliers}
\alias{detect_het_outliers}
\title{Diagnose heterozygosity outliers and genotype miscall}
\usage{
detect_het_outliers(
  data,
  strata = NULL,
  group.column = "STRATA",
  z.threshold = 4,
  estimate.miscall = TRUE,
  nreps = 2000L,
  burn.in = 500L,
  max.mcmc.markers = 10000L,
  seed = 1L,
  chunk.size = 2000L,
  write.files = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A GDS filepath or open \code{SeqVarGDSClass} object.}

\item{strata}{Optional metadata containing \code{INDIVIDUALS} and \code{group.column}.}

\item{group.column}{Metadata column defining strata.}

\item{z.threshold}{Absolute heterozygote-residual z-score used to flag
marker-stratum combinations.}

\item{estimate.miscall}{Fit the heterozygote-miscall MCMC model.}

\item{nreps}{Number of MCMC iterations.}

\item{burn.in}{Number of initial MCMC iterations discarded.}

\item{max.mcmc.markers}{Maximum markers used by the MCMC model.}

\item{seed}{Random seed used for marker subsampling and MCMC.}

\item{chunk.size}{Number of variants read at a time.}

\item{write.files}{Write statistics and figures.}

\item{verbose}{Display progress and summary messages.}

\item{...}{Common arguments: \code{path.folder} and \code{internal}.}
}
\value{
A \code{detect_het_outliers} object containing marker statistics,
outliers, MCMC results when requested, plots, and output information.
}
\description{
Compare observed and Hardy-Weinberg expected genotype frequencies for active
biallelic GDS markers. The function reports standardized heterozygote
residuals by stratum and optionally fits the Anderson heterozygote-miscall
model by MCMC to a reproducible marker subset.
}
\details{
The miscall model assumes true heterozygotes can be observed as either
homozygote with a common probability. Population structure, inbreeding,
null alleles, paralogy, allele dropout, and batch effects can violate this
model. Its estimate is therefore a diagnostic under explicit assumptions,
not a universal sequencing-error rate.
}
\examples{
\dontrun{
outliers <- radr::detect_het_outliers(
  "study.gds", strata = "sample_metadata.tsv", burn.in = 500
)
}
}
\author{
Eric Anderson \email{eric.anderson@noaa.gov} and Thierry Gosselin
\email{thierrygosselin@icloud.com}
}
