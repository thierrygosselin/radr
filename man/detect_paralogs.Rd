% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_paralogs.R
\name{detect_paralogs}
\alias{detect_paralogs}
\title{Detect candidate paralogous markers}
\usage{
detect_paralogs(
  data,
  strata = NULL,
  group.column = "STRATA",
  by.strata = TRUE,
  min.call.rate = 0.8,
  min.samples.per.group = 10L,
  min.heterozygotes = 5L,
  min.heterozygote.depth = 50,
  heterozygosity.threshold = 0.5,
  deviation.threshold = 4,
  chunk.size = 2000L,
  save.plots = TRUE,
  plot.formats = c("png", "pdf"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A GDS filepath or an open \code{SeqVarGDSClass} object.}

\item{strata}{Optional data frame or tab-delimited metadata file containing
\code{INDIVIDUALS} and \code{group.column}. If \code{NULL}, metadata are read from
the GDS.}

\item{group.column}{Metadata column used for stratified diagnostics.}

\item{by.strata}{Calculate diagnostics independently within each group in
addition to the pooled analysis.}

\item{min.call.rate}{Minimum called-genotype proportion required.}

\item{min.samples.per.group}{Minimum active samples required in a diagnostic
group.}

\item{min.heterozygotes}{Minimum heterozygous genotypes with usable
allele-depth values required for read-ratio inference.}

\item{min.heterozygote.depth}{Minimum pooled REF plus ALT depth across
heterozygotes.}

\item{heterozygosity.threshold}{Minimum \code{H_CALLED} for excess
heterozygosity evidence.}

\item{deviation.threshold}{Minimum \code{ABS_D} for read-imbalance evidence.}

\item{chunk.size}{Number of active variants read from the GDS at a time.}

\item{save.plots}{Save HD diagnostic plots.}

\item{plot.formats}{One or both of \code{"png"} and \code{"pdf"}.}

\item{verbose}{Display progress and summary messages.}

\item{...}{Common arguments: \code{path.folder} and \code{internal}.}
}
\value{
A \code{detect_paralogs} object containing:
\itemize{
\item \code{statistics}: pooled marker statistics and cross-stratum evidence;
\item \code{strata.statistics}: pooled and group-specific HD statistics;
\item \code{candidates}: markers flagged in the pooled data or any stratum;
\item \code{thresholds}: screening and minimum-evidence settings;
\item \code{plots}: generated ggplot objects;
\item \code{path.folder} and \code{output.files};
\item \code{active.selection.restored}: filter-restoration check.
}
The function is read-only and does not change the persistent GDS filters.
}
\description{
\code{detect_paralogs()} implements a conservative, GDS-based version of the
HDplot diagnostic described by McKinney et al. (2017). It combines the
proportion of called samples that are heterozygous (\code{H_CALLED}) with the
deviation of pooled REF and ALT reads from a 1:1 ratio among heterozygous
genotypes (\code{D}). Candidate labels prioritize review; they are not proof of
paralogy.
}
\details{
Excess heterozygosity or allele imbalance can also be caused by population
mixture, sex linkage, copy-number variation, contamination, allele dropout,
mapping bias, collapsed repeats, or genotype-calling errors. Confirm
candidates with population consistency, sequence or reference placement,
segregation, copy-number, and assembly evidence whenever possible.
}
\section{HD statistics}{

The function reports both heterozygosity definitions from the published
implementation. \code{H_ALL} divides the number of heterozygotes by all samples
and is sensitive to missingness. \code{H_CALLED} divides by called samples and is
the default HDplot x-axis. \code{D} is the signed binomial z-score for departure
from equal REF and ALT read counts among heterozygotes; \code{ABS_D} is used for
symmetric candidate screening. MAF is calculated from called diploid
dosages in the active sample set.
}

\section{Candidate categories}{

Each pooled or stratum-specific marker receives one status:
\itemize{
\item \code{INSUFFICIENT_DATA}: one or more evidence requirements failed;
\item \code{SINGLETON_LIKE}: neither screening threshold was crossed;
\item \code{EXCESS_HET_ONLY}: only the heterozygosity threshold was crossed;
\item \code{READ_IMBALANCE_ONLY}: only the read-deviation threshold was crossed;
\item \code{PARALOG_CANDIDATE}: both thresholds were crossed.
}
These empirical thresholds are screening controls, not universal paralog
boundaries. Inspect the full distribution, MAF, missingness, depth, strata,
and technical metadata before removing markers.
}

\section{Strata}{

With \code{by.strata = TRUE}, diagnostics are calculated for the pooled active
samples and independently for each value of \code{group.column}. A signal that
occurs in only one population or technical group deserves additional review:
it can reflect real lineage-specific duplication, but also population
structure, batch effects, or small group size. Pooled and stratified results
are therefore both retained.
}

\examples{
\dontrun{
paralogs <- radr::detect_paralogs(
  data = "study.gds",
  strata = "sample_metadata.tsv",
  group.column = "STRATA"
)
paralogs$candidates
}
}
\references{
McKinney GJ, Waples RK, Seeb LW, Seeb JE (2017). Paralogs are revealed by
proportion of heterozygotes and deviations in read ratios in
genotyping-by-sequencing data from natural populations. \emph{Molecular Ecology
Resources}, 17, 656-669. https://doi.org/10.1111/1755-0998.12613
}
\seealso{
\code{\link{detect_het_outliers}}, \code{\link{detect_mixed_genomes}},
\code{\link{sexy_markers}}
}
\author{
Thierry Gosselin \email{thierrygosselin@icloud.com}
}
