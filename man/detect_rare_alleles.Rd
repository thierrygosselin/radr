% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect_rare_alleles.R
\name{detect_rare_alleles}
\alias{detect_rare_alleles}
\title{Evaluate read support for rare alleles}
\usage{
detect_rare_alleles(
  data,
  strata = NULL,
  group.column = "STRATA",
  by.strata = TRUE,
  max.minor.allele.count = 3L,
  min.call.rate = 0.8,
  min.samples.per.group = 10L,
  min.depth.completeness = 0.8,
  min.minor.read.depth = 3,
  min.total.minor.depth = 6,
  max.low.support.fraction = 0.5,
  min.heterozygote.balance = 0.2,
  min.heterozygotes.balance = 1L,
  chunk.size = 2000L,
  save.plots = TRUE,
  plot.formats = c("png", "pdf"),
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A GDS filepath or open \code{SeqVarGDSClass} object.}

\item{strata}{Optional sample metadata data frame or tab-delimited file.}

\item{group.column}{Metadata column used for stratified diagnostics.}

\item{by.strata}{Calculate pooled and group-specific statistics.}

\item{max.minor.allele.count}{Largest minor-allele count classified as rare.}

\item{min.call.rate}{Minimum called-genotype proportion.}

\item{min.samples.per.group}{Minimum samples in a diagnostic group.}

\item{min.depth.completeness}{Minimum proportion of carriers with REF and
ALT depth.}

\item{min.minor.read.depth}{Minimum minor-allele depth in one carrier.}

\item{min.total.minor.depth}{Minimum summed minor depth across carriers.}

\item{max.low.support.fraction}{Maximum proportion of weakly supported
carriers.}

\item{min.heterozygote.balance}{Lower acceptable minor-read fraction in
heterozygotes; the upper bound is one minus this value.}

\item{min.heterozygotes.balance}{Minimum heterozygotes with depth required
for the balance check.}

\item{chunk.size}{Number of active variants processed per GDS chunk.}

\item{save.plots}{Save diagnostic plots.}

\item{plot.formats}{One or both of \code{"png"} and \code{"pdf"}.}

\item{verbose}{Display concise progress messages.}

\item{...}{Common arguments: \code{path.folder} and \code{internal}.}
}
\value{
A \code{detect_rare_alleles} object with marker and stratum
statistics, review candidates, thresholds, plots, output paths, depth
provenance, and confirmation that the incoming GDS filter was restored.
}
\description{
\code{detect_rare_alleles()} identifies rare alleles and evaluates their
allele-specific read support. It does not diagnose null alleles or allele
dropout directly, and it does not remove markers. The observed minor allele
is recalculated from the active samples, so results do not depend on whether
that allele is encoded as REF or ALT.
}
\section{Evidence categories}{

\itemize{
\item \code{MONOMORPHIC}: no minor allele is present;
\item \code{NOT_RARE}: minor-allele count exceeds the requested maximum;
\item \code{INSUFFICIENT_DATA}: sample size or call rate is inadequate;
\item \code{SUPPORTED_RARE}: the rare allele passes the requested checks;
\item \code{LOW_READ_SUPPORT}: carrier depth is incomplete or weak;
\item \code{ALLELE_BALANCE_WARNING}: heterozygote balance is outside bounds;
\item \code{DEPTH_AND_BALANCE_WARNING}: both warnings are present.
}
These are review categories, not universal biological boundaries.
Sequencing error, mapping bias, dropout, contamination, paralogy, batch
effects, and genuine rare variation can produce overlapping patterns.
}

\section{Stratified diagnostics}{

With \code{by.strata = TRUE}, statistics are calculated in the pooled samples and
independently within each value of \code{group.column}. Minor-allele identity is
inferred within each group and can switch between REF and ALT.
}

\examples{
\dontrun{
support <- radr::detect_rare_alleles(
  data = "study.gds", strata = "sample_metadata.tsv"
)
support$candidates
}
}
\seealso{
\code{\link{detect_paralogs}},
\code{\link{detect_biallelic_problems}}, \code{\link{filter_ma}}
}
\author{
Thierry Gosselin \email{thierrygosselin@icloud.com}
}
