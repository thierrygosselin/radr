% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sex_markers.R
\name{sexy_markers}
\alias{sexy_markers}
\title{Screen a GDS for candidate sex-linked markers}
\usage{
sexy_markers(
  data,
  strata = NULL,
  sex.column = "STRATA",
  presence.threshold = 0.9,
  absence.threshold = 0.1,
  min.heterozygosity.difference = 0.2,
  coverage.ratio.threshold = 1.5,
  coverage.threshold = 1,
  min.samples.per.sex = 5L,
  fdr.threshold = 0.05,
  require.significance = FALSE,
  chunk.size = 2000L,
  save.plots = TRUE,
  plot.formats = c("png", "pdf"),
  folder.name = NULL,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A GDS filepath or an open \code{SeqVarGDSClass} object.}

\item{strata}{A data frame or tab-delimited file containing \code{INDIVIDUALS}
and \code{sex.column}. If \code{NULL}, individual metadata are read from the GDS.}

\item{sex.column}{Metadata column containing sex. \code{F}, \code{female}, \code{M}, and
\code{male} are recognized without regard to case; other values are unknown.}

\item{presence.threshold}{Minimum presence in the expected sex for Y/W.}

\item{absence.threshold}{Maximum presence in the other sex for Y/W.}

\item{min.heterozygosity.difference}{Minimum absolute female-minus-male
heterozygosity difference for X/Z candidates.}

\item{coverage.ratio.threshold}{Minimum larger-to-smaller normalized depth
ratio for X/Z coverage candidates.}

\item{coverage.threshold}{Minimum read depth considered present. Without
depth data, a non-missing genotype is considered present.}

\item{min.samples.per.sex}{Minimum number of known females and males.}

\item{fdr.threshold}{Maximum FDR when significance is required.}

\item{require.significance}{Require effect size and method-specific FDR.}

\item{chunk.size}{Number of variants read from the GDS at a time.}

\item{save.plots}{Save diagnostic plots in the result folder.}

\item{plot.formats}{One or both of \code{"png"} and \code{"pdf"}.}

\item{folder.name}{Optional result-folder stem. Default: \code{sexy_markers}.}

\item{verbose}{Display progress and summary messages.}

\item{...}{Common arguments: \code{path.folder} and \code{internal}.}
}
\value{
A \code{sexy_markers} object containing marker statistics, candidates,
an assignment-ready Y/W panel, sample summaries, a metadata audit, plots,
and the result-folder path.
}
\description{
\code{sexy_markers()} compares marker presence, heterozygosity, and, when
available, normalized read depth between known females and males. Dominant
SilicoDArT markers stored with \code{MARKER_TYPE = SILICODART} contribute
presence/absence evidence but are not interpreted as diploid heterozygosity.
It is a
read-only screen: active GDS sample and variant selections are respected and
restored, and no filter is applied or written to the GDS.
}
\details{
Y-like markers are present mainly in males, W-like markers mainly in females,
X-like markers have greater heterozygosity or normalized depth in females,
and Z-like markers show the reverse. These labels are candidates, not proof
of chromosomal location. Sex-specific dropout, population structure, plate
or library effects, paralogy, and mapping bias can produce similar patterns.
}
\section{Statistical tests}{

Presence and heterozygosity use two-sample score tests for proportions.
Normalized depth uses Welch tests on \code{log2(1 + depth)}. P-values are adjusted
separately by method with Benjamini-Hochberg FDR. Candidate selection always
uses explicit effect-size thresholds; \code{require.significance = TRUE} also
requires the method-specific FDR to pass \code{fdr.threshold}.
}

\section{Read-depth normalization}{

When depth is available, each sample is divided by its mean positive depth
across active markers. This reduces sample-wide sequencing-depth differences
but cannot remove marker-by-batch interactions or confounding between sex and
plate, lane, library, population, or sampling group.
}

\examples{
\dontrun{
result <- radr::sexy_markers(
  data = "study.gds",
  strata = "sample_metadata.tsv",
  sex.column = "SEX"
)
result$candidates
}
}
\seealso{
Run \code{vignette("detecting_sex_linked_markers", package = "radr")} for the
biological background, guidance on discovery data, SilicoDArT and VCF
workflows, interpretation, alternative methods, and validation.
}
