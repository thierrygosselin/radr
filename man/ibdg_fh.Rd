% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ibdg_fh.R
\name{ibdg_fh}
\alias{ibdg_fh}
\title{Estimate individual genomic inbreeding with FH}
\usage{
ibdg_fh(
  data,
  strata = NULL,
  group.column = "STRATA",
  chunk.size = 2000L,
  min.call.rate = 0,
  write.files = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A GDS filepath or open \code{SeqVarGDSClass} object.}

\item{strata}{Optional metadata containing \code{INDIVIDUALS} and \code{group.column}.}

\item{group.column}{Metadata column defining strata.}

\item{chunk.size}{Number of variants read at a time.}

\item{min.call.rate}{Minimum individual call rate retained in summaries.}

\item{write.files}{Write result tables and figures.}

\item{verbose}{Display progress and summary messages.}

\item{...}{Common arguments: \code{path.folder} and \code{internal}.}
}
\value{
An \code{ibdg_fh} object with individual and stratum statistics, plots,
and output information.
}
\description{
Estimate the method-of-moments FH coefficient from active biallelic GDS
genotypes. For each individual, observed homozygous genotype counts are
compared with expected homozygous counts calculated from allele frequencies
at exactly the markers called in that individual.
}
\details{
The implemented estimator is \code{(O - E) / (N - E)}, matching the count-scale
form used by PLINK's heterozygosity report. \code{FH_STRATUM} uses allele
frequencies from the individual's stratum; \code{FH_POOLED} uses frequencies
from all active samples. These are genomic method-of-moments estimators, not
direct proof that chromosome segments are identical by descent.
}
\examples{
\dontrun{
fh <- radr::ibdg_fh("study.gds", strata = "sample_metadata.tsv")
}
}
\references{
Keller MC, Visscher PM, Goddard ME (2011). Quantification of
inbreeding due to distant ancestors and its detection using dense SNP data.
\emph{Genetics}, 189, 237-249.

Kardos M, Luikart G, Allendorf FW (2015). Measuring individual
inbreeding in the age of genomics. \emph{Heredity}, 115, 63-72.
}
\author{
Thierry Gosselin \email{thierrygosselin@icloud.com}
}
