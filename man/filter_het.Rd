% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_het_gds.R
\name{filter_het}
\alias{filter_het}
\title{Filter heterozygosity outliers}
\usage{
filter_het(
  data,
  strata = NULL,
  group.column = "STRATA",
  individual.range = NULL,
  marker.range = NULL,
  strata.threshold = 1L,
  min.individual.call.rate = 0,
  min.marker.call.rate = 0.8,
  chunk.size = 2000L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{A GDS filepath or open \code{SeqVarGDSClass} object.}

\item{strata}{Optional metadata containing \code{INDIVIDUALS} and \code{group.column}.}

\item{group.column}{Metadata column defining strata.}

\item{individual.range}{Permitted individual observed-heterozygosity range.
Use \code{NULL} to retain all individuals.}

\item{marker.range}{Permitted marker observed-heterozygosity range within
strata. Use \code{NULL} to retain all markers.}

\item{strata.threshold}{Number or proportion of eligible strata that must
violate \code{marker.range} before marker removal.}

\item{min.individual.call.rate}{Minimum individual call rate.}

\item{min.marker.call.rate}{Minimum marker call rate within a stratum.}

\item{chunk.size}{Number of variants read at a time.}

\item{verbose}{Display progress and summary messages.}

\item{...}{Common argument \code{path.folder}.}
}
\value{
The filtered open GDS connection with complete filter audit files.
}
\description{
Filter active GDS individuals and markers using observed heterozygosity.
Individual statistics are calculated first. When individuals are removed,
marker statistics are recalculated from the remaining active samples before
marker filtering. This avoids applying stale marker heterozygosity estimates.
}
\examples{
\dontrun{
genome <- radr::filter_het(
  genome, strata = "sample_metadata.tsv",
  individual.range = c(0.05, 0.35), marker.range = c(0, 0.8)
)
}
}
\author{
Thierry Gosselin \email{thierrygosselin@icloud.com}
}
